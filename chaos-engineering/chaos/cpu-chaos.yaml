apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress
  namespace: chaos-engineering
spec:
  mode: one
  selector:
    labelSelectors:
      app: chaos-app
  stressors:
    cpu:
      workers: 2
      load: 90
  duration: "60s"
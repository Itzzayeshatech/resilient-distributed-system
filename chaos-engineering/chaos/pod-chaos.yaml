apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill
  namespace: chaos-engineering
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: chaos-app
  duration: "30s"
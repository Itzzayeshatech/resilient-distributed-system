apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: memory-stress
  namespace: chaos-engineering
spec:
  mode: one
  selector:
    labelSelectors:
      app: chaos-app
  stressors:
    memory:
      workers: 2
      size: "500MB"
  duration: "60s"
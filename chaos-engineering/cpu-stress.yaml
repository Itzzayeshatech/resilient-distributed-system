apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress
  namespace: chaos-engineering
spec:
  mode: one
  selector:
    namespaces:
      - chaos-engineering
  stressors:
    cpu:
      workers: 4
      load: 90
  duration: '60s'